// Introduces a new attribute to documents within the 'school' collection.

db.school.update(
    { name: "Holberton school" },
    { "$set": { address: "972 Mission street" } },
    { multi: true }
);

